services:
  mongo:
    image: mongo:7
    container_name: recipe-hub-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: recipe_hub
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
